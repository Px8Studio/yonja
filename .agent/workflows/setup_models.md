---
description: Setup and pull required Ollama models
---

This workflow initializes the Docker environment for model setup and pulls the required models.

1. Run model setup.
// turbo
2. Executing command: `docker-compose -f docker-compose.local.yml --profile setup up model-setup`
