---
description: Start all ALİM services (Docker, FastAPI, LangGraph, UI)
---

This workflow starts the entire ALİM stack using the `start_all.ps1` script.

1. Start all services in the background.
// turbo
2. Executing command: `pwsh -File scripts/start_with_logs.ps1`
