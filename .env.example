# 🌿 YONCA AI — Environment Configuration Template
# ==============================================================================
# INSTRUCTIONS:
# 1. Copy this file to `.env` (cp .env.example .env)
# 2. Fill in the values for your local environment
# 3. NEVER commit the `.env` file to git!
# ==============================================================================

# 🚀 API & Service URLs
# ------------------------------------------------------------------------------
YONCA_API_URL=http://localhost:8000
YONCA_LANGGRAPH_BASE_URL=http://localhost:2024
YONCA_LANGGRAPH_GRAPH_ID=yonca_agent
# Set to 'false' if running without LangGraph for pure UI testing
YONCA_LANGGRAPH_REQUIRED=true

# 🧠 LLM Provider Settings (Ollama)
# ------------------------------------------------------------------------------
# Provider can be 'ollama', 'openai', etc. (Currently optimized for local Ollama)
LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
# Recommended models: qwen2.5:14b, mistral-nemo, llama3
OLLAMA_MODEL=qwen2.5:14b

# 💾 Database & Storage
# ------------------------------------------------------------------------------
# Main Database (Postgres recommended for persistence)
# Format: postgresql+asyncpg://user:pass@host:5432/dbname
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/yonca

# Chainlit Data Layer (Optional - defaults to DATABASE_URL if empty)
CHAINLIT_DATABASE_URL=

# Enable persisting chat history/feedback (Requires Postgres)
ENABLE_DATA_PERSISTENCE=true

# 🔐 Authentication (Google OAuth)
# ------------------------------------------------------------------------------
# Leave empty to disable authentication (open access)
OAUTH_GOOGLE_CLIENT_ID=
OAUTH_GOOGLE_CLIENT_SECRET=
# Generate a random string for session security if Auth is enabled
CHAINLIT_AUTH_SECRET=

# 🛠️ Integration Settings
# ------------------------------------------------------------------------------
# 'api' = Connect to FastAPI backend
# 'direct' = (Deprecated) Direct library import
INTEGRATION_MODE=api
